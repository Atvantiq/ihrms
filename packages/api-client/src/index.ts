/**
 * Typed API contract, generated from the FastAPI OpenAPI spec.
 *
 * Regenerate after backend changes (API must be running):
 *   npm run generate --workspace packages/api-client
 *
 * Use `paths` / `components` to type fetch calls so the web app cannot
 * drift from the API contract.
 */
export type { components, operations, paths } from "./schema";
